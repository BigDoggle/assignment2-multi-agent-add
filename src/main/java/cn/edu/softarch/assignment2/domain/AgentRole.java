package cn.edu.softarch.assignment2.domain;

/**
 * Defines the fixed agents that participate in each ADD workflow step.
 */
public enum AgentRole {
    COORDINATOR,
    ARCHITECT,
    QUALITY_ANALYST,
    REVIEWER,
    RECORDER
}
